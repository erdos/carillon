(set def
     (lit mac nil (name args body)
          `(set ,name (lit clo nil ,args ,body))))

(set mac
   (lit mac nil (name args body)
        `(set ,name (lit mac nil ,args ,body))))

(def join (x y) (join x y))
(def car (x y) (join x y))
(def cdr (x y) (join x y))